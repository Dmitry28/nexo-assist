# Product Plan — Listing Watch Bot

Дорожная карта: превращаем прототип `land-scraper` в многопользовательский
сервис на скелете **nexo-assist** (NestJS 11). Логику парсинга портируем из
land-scraper с рефакторингом. Развиваем по фазам — от простого локального к
распределённому. Как продукт работает — в [PRODUCT.md](PRODUCT.md).

---

## 1. Принципы

- По фазам: от простого локального к сложному распределённому.
- Best practices: Docker/k8s, секреты, observability (otel/prometheus/pino в
  скелете), миграции БД, тесты на каждый адаптер.
- Каждая фаза: на входе детально изучаем и обсуждаем варианты; этот документ —
  только высокоуровневый каркас.

## 2. Решения

| Тема                    | Решение                                                                                                                                                                                                                                |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Telegram UX (старт)     | Личный чат, управление **кнопками**, не `/add`.                                                                                                                                                                                        |
| Telegram UX (позже)     | Группа + forum-топики (топик на подписку). Возможно Mini App.                                                                                                                                                                          |
| События уведомлений     | Сейчас только **new**; removed/price — Фаза 2.4; выбор на подписку — Фаза 7.                                                                                                                                                           |
| Хостинг                 | Приложение — **Hetzner** + self-managed **k3s** (Oracle Free ARM не дали: out of capacity). Прокси скрейпинга — Oracle Always Free ($0). Детали — [PRODUCT_TECH.md](PRODUCT_TECH.md).                                                  |
| Трекер новых источников | GitHub Issues → LLM генерит адаптер.                                                                                                                                                                                                   |
| Репозиторий             | `nexo-assist` (этот репо и есть продукт).                                                                                                                                                                                              |
| Bot transport           | Переключаемый: long-polling (сейчас) → webhook — при горизонтальном масштабировании (позже).                                                                                                                                           |
| Postgres                | Локально — Docker; прод — managed **Supabase** (Neon оказался недоступен); позже для практики — свой Postgres в кластере (сначала на dev).                                                                                             |
| ORM                     | TypeORM. Миграции только генерируемые, `synchronize: true` в проде запрещён.                                                                                                                                                           |
| Git-флоу                | От `dev` → мерж в `dev` → тест → только потом `main`.                                                                                                                                                                                  |
| Ревью перед PR          | `/logic-review` + `/review-code` **вместе**; оба только сообщают, правки идут обычным изменением. `/refine` удалён — его ценность была в моменте запуска, а не в критериях; стал § Whole-Change Pass + перезапуском (шаг 6 чек-листа). |

## 3. Фазы

### Фаза 0 — Основа, контракты, документация

- Завести `docs/PRODUCT.md` (создан; держать актуальным).
- Описать `SourceAdapter`, `NormalizedListing`, реестр адаптеров — типы и каркас,
  без реализации сайтов.
- Fixtures-подход для тестов адаптеров (как в `land-scraper/__tests__`).

### Фаза 1 — Локальный MVP (без БД) — **сделано (kufar)**

- Telegram-бот на long-polling (grammY).
- **kufar**-адаптер: fetch + parse `__NEXT_DATA__`, первая страница.
- Подписки и «видели» — in-memory; уведомления только «новые», дайджест с капом.
- baseline на первой подписке; «видели» помечается только после доставки.
- Ежедневный крон (`WATCH_CRON`); dev-команда `/check`.
- Перенесено в Фазу 2: realt-адаптер, инкрементальная пагинация (стоп на виденных).

### Фаза 2 — Плагинная архитектура

Делаем под-шагами:

- **2.1 — сделано:** `SourceAdapter` + `SourceRegistry`, kufar переехал в `sources/kufar/`
  адаптер; ядро (`WatchService`/scheduler) ходит через реестр; контракт пока lean
  (`id`/`matches`/`fetch`); unit-тесты на адаптер с фикстурами.
- **2.2 — сделано:** realt-адаптер (2-й источник) + матчинг/регистрация.
- **2.3 — сделано:** пагинация с лимитом страниц (page-cap), newest-first.
  Стоп-на-виденных (ранний выход) — опциональная оптимизация позже.
- **2.4 — нормализация URL сделано:** `normalizeUrl` (host/www, отбрасывает волатильные
  и `utm_*` params, сортирует остальные, trailing slash) + колонка `normalizedUrl`,
  уникальный индекс `(userId, normalizedUrl)`, дедуп в `add()`.
- **2.4 (позже):** события removed / price (+ `capabilities`) — ждут таблицу `listings`/
  price-history (отдельный «шаг дедупа» из Фазы 3).

### Фаза 3 — БД и мультипользовательность

Цель: заменить in-memory на Postgres и вынести юзеров в отдельную сущность.
Минимально: без `listings`/price-history и без дедупа-по-URL — это отдельный
шаг позже. Делаем под-шагами.

**Решения фазы:**

- Один `DATABASE_URL` (Neon отдаёт URL). Локально — Docker; staging/prod — Neon
  (SSL обязателен, использовать pooled-endpoint; учесть дроп idle-коннектов).
- Только генерируемые миграции; `synchronize: false` всегда. Отдельный
  `data-source.ts` для CLI (не переиспользуем `TypeOrmModule.forRootAsync`).
- `source` — `varchar`, не PG-enum (новый источник не должен требовать миграцию типа).
- UUID PK для `users`/`subscriptions` (уже так); `seen_listings` — составной PK
  `(subscription_id, external_id)` без суррогатного id (он же и индекс проверки).
  Timestamps; `baselinedAt` nullable. FK — `ON DELETE CASCADE`.
- Тесты: юнит — лёгкий in-memory fake репозитория (быстро, без БД); e2e/интеграция —
  реальный Postgres из docker-compose (сервис в CI).

**3.1 — Инфра БД — сделано.** 3.1a: Postgres в `docker-compose.yml` (+ healthcheck)
и скрипты `db:up/down/reset`. 3.1b: зависимости (`@nestjs/typeorm typeorm pg`),
`DATABASE_URL` в env.validation + configuration + `.env.example`,
`TypeOrmModule.forRootAsync` (inject `AppConfig`, `autoLoadEntities`,
`synchronize:false`, SSL для не-локальных), readiness-проба `db.pingCheck`, сервис
Postgres в CI. (k8s Secret для `DATABASE_URL` — в Фазе 5, деплой.)

**3.2 — Схема + первая миграция — сделано.** Сущности `Subscription` (uuid PK,
`telegramUserId`, source, url, createdAt, `baselinedAt`) и `SeenListing` — составной
PK `(subscriptionId, externalId)` (он же индекс проверки «видели/нет»), `seenAt`, FK
→ subscription `ON DELETE CASCADE`. `data-source.ts` (CLI) + скрипты
`migration:generate|run|revert|show` (ts-node/tsconfig-paths) + первая миграция
`InitSchema`. `User` + FK `subscription → user` + `forFeature` — в 3.3 (вместе с их
первым потребителем).

**3.3 — Перенос сервисов на БД (async) — сделано.** `SubscriptionsService` через
`@InjectRepository` + `TypeOrmModule.forFeature`; методы стали `async` → await в
`WatchService`, `telegram.handlers`, `watch.scheduler`. `getSeen` — запрос по кандидатам
прогона (`external_id = ANY(ids)`), не загрузка всего сета. `baseline` (seed +
mark-baselined) — в одной транзакции (`seedBaseline`). Удаление подписки каскадит
`seen` (FK). Тесты: слой данных → интеграционный на реальном Postgres (`test/`), юниты
`handlers`/`scheduler` — на моках коллабораторов. Ключ пока — `telegramUserId`.

**3.3b — Таблица `User` + профиль — сделано.** `User`-сущность (аккаунт: `telegramId`
unique — первая identity; username, имя, язык; веб-авторизация позже расширяет тот же
ряд); `Subscription` → `user` (eager, FK `ON DELETE CASCADE`), `telegramUserId` убран.
Upsert профиля при подписке (`ctx.from` → `users`). Миграция `AddUsers`. Ключ снаружи
остаётся telegram-id (сервис резолвит в user). Профиль пока не читается — собираем
заранее (при подписке `ctx.from` есть, потом не восстановить).

**3.4 — Чистка `seen` по окну — сделано.** Бот смотрит только первые несколько страниц
поиска (page-cap ≈ 150 самых свежих); всё, что уехало дальше, он больше не запрашивает —
значит и «новым» показаться не может. Поэтому храним только последние `N` виденных id на
подписку (`MAX_SEEN_PER_SUBSCRIPTION=300`, с запасом к page-cap), старые удаляем прунингом
после каждой вставки. Таблица на подписку ограничена навсегда, без риска повторно прислать
старое.

**3.5 — Лимиты / анти-абьюз — сделано.** Max подписок на юзера
(`MAX_SUBSCRIPTIONS_PER_USER=50`, проверка в `add`) + запрет дублей по точному URL;
дружелюбные сообщения в боте (лимит / «уже отслеживаешь»). Полная уникальность
`(user_id, normalized_url)` + DB-констрейнт — со шагом нормализации URL (Фаза 2.4).

**3.6 (опц.) — In-run fetch-кэш.** Несколько подписок на один URL в одном прогоне →
один fetch (Map на прогон). Дешёвый предшественник дедупа; можно перенести в Фазу 4.

**Отдельным шагом (позже, вне Фазы 3):** дедуп-по-URL (один скрейп на нормализованный
URL → fan-out) и вместе с ним `listings` + **снимки цен** (price history) — фундамент
для removed/price-событий (2.4), триггеров «цена упала» (Фаза 7) и аналитики (Фаза 8).

### Фаза 4 — Резилентность планировщика + observability

Есть база: `WatchScheduler.runDaily` уже опрашивает подписки с изоляцией сбоев и
дайджест-доставкой. Добавляем резилентность и наблюдаемость маленькими шагами.
Тяжёлая инфра (прокси-бюджет, OTLP-экспорт, «поднять частоту») завязана на живой
деплой — уезжает в Фазу 5.

- **4.1 — Пейсинг скрейпа — сделано:** пауза `WATCH_MIN_DELAY_MS + random(WATCH_JITTER_MS)`
  между поллами в `runDaily` (последовательно, без knob на параллелизм — добавим,
  только когда упрёмся в скорость). Без миграций.
- **4.2 — Состояние подписки + автопауза на 403 — сделано:** колонка `pausedAt`,
  планировщик берёт только активные (`listActive`); Telegram `403` (заблокировал/удалён) →
  пауза подписок юзера (сбой записи не роняет прогон); `429` — плагином
  `@grammyjs/auto-retry`. Снятие с паузы: повторная отправка ссылки оживляет подписку
  (в `add()`); авто-unpause / команда `/resume` — позже.
- **4.3 — Детект «мёртвой» ссылки — сделано:** счётчик `consecutiveFailures` (только
  ошибки опроса, не «0 результатов» — иначе ложные срабатывания на узких поисках);
  N ошибок подряд (порог 5) → уведомить юзера «обнови ссылку» + автопауза подписки;
  сброс при успехе. Чистка `seen` уже покрыта prune до `MAX_SEEN_PER_SUBSCRIPTION`.
- **4.4 — Продуктовые метрики — сделано:** Prometheus counters (`nexo_deliveries_total`,
  `nexo_poll_errors_total`, `nexo_subscriptions_paused_total`) + gauges (`nexo_users`,
  `nexo_active_subscriptions`) через фасад `WatchMetrics`; инкременты в планировщике,
  снимок totals раз в прогон.
- **4.5 — Админ-команда `/stats` — сделано:** только для админа (`ADMIN_TELEGRAM_ID`,
  иначе молча игнор) — юзеры / активные / на паузе / последний прогон (`WatchStatus`).
- **4.6 — Алерты админу — сделано:** нотификация владельцу при каждой автопаузе
  (403 / мёртвая ссылка) с причиной; плюс алерт, если источник завалил все поллы за
  прогон (≥3, чтобы одна битая ссылка не поднимала ложную тревогу). Переиспользует
  админ-id (4.5). Экспорт ошибок в OTLP — Фаза 5.

### Фаза 5 — Деплой — **сделано** (детали инфраструктуры: [PRODUCT_TECH.md](PRODUCT_TECH.md))

Продукт работает 24/7 в проде. Что сделано:

- **Стек:** приложение — **Hetzner CX23 + self-managed k3s** (Oracle Always Free ARM не дали:
  out of capacity во всех AD); БД — managed **Supabase** (Neon не открылся); egress-прокси для
  скрейпинга — бесплатный **Oracle Always Free**; манифесты — Kustomize; образ — multi-arch в GHCR.
  ~€6.5/мес, остальное $0.
- **Миграции при деплое:** `data-source.ts` сам понимает, где запущен (`.ts` под ts-node,
  `.js` в контейнере) → `migration:run:prod` гоняется **initContainer**'ом до старта бота.
  Упали миграции → под не поднялся.
- **Секреты:** k8s Secret (`TELEGRAM_BOT_TOKEN`, `DATABASE_URL`, `SCRAPE_PROXY_URL`,
  `SENTRY_DSN`), заводятся `npm run k8s:secrets` (скрытый ввод). Владелец держит копию в
  менеджере паролей. Правильное решение (зашифрованные в git) — SOPS+age, см. ниже.
- **Безопасность БД:** TLS с проверкой сертификата (`sslmode=verify-full` + CA провайдера
  через `NODE_EXTRA_CA_CERTS`); публичный Data API провайдера **выключен** + **RLS** на всех
  таблицах (миграция) — два независимых рубежа.
- **Один Deployment:** long-polling бот + in-process scheduler (singleton, `Recreate`).
  Отдельный scrape-CronJob и webhook — только под горизонтальное масштабирование.
- **CI/CD:** мерж в `dev` → тесты → мультиарх-образ в GHCR (тег по sha) → джоба `deploy`
  заходит по SSH ключом, ограниченным одной командой, и выкатывает тот же коммит
  ([`deploy/deploy.sh`](../deploy/deploy.sh)). API кластера наружу закрыт, kubeconfig в GitHub
  не хранится. Подготовка/переезд хоста — [`deploy/setup-server.sh`](../deploy/setup-server.sh).
- **Прокси на уровне источника:** `kufar.by` блокирует IP-диапазоны **хостингов** (не страну:
  VPN-Польша и GitHub Actions проходят, Hetzner — нет). Адаптер объявляет `useProxy: true`,
  транспорт берёт адрес из `SCRAPE_PROXY_URL`. Новый источник → одна строка; смена прокси →
  одна переменная. `realt.by` ходит напрямую.
- **Ошибки долетают до владельца:** продуктовые события (автопаузы, провал источника) — в
  Telegram (Фаза 4); **любая ошибка приложения** — в **Sentry**: необработанные исключения
  (с ожиданием отправки до выхода), HTTP 5xx и намеренно проглоченные ошибки прогона.
  Только ошибки, без трейсинга/профилирования — квота бесплатного тарифа не тратится зря.
- **Видно, кого именно задело:** общий репортер `telegram/report.ts` цепляет к отчёту
  пользователя и теги (вокабуляр — в [PRODUCT_TECH.md](PRODUCT_TECH.md)). Подключены все
  пользовательские пути и ежедневный прогон — раньше это жило только в логах, которые
  теряются при перезапуске пода.

**Осталось по фазе:**

- **Наблюдаемость: разделить админа и адресата алертов.** `ADMIN_TELEGRAM_ID` сейчас отвечает и
  за права на `/stats`, и за адрес алертов — вписать туда канал нельзя, `/stats` сломается. Ввести
  `ALERT_CHAT_ID` (по умолчанию = админ): тогда переезд алертов в канал/группу — смена переменной,
  без правок кода. Актуально, когда смотреть будет не только владелец.
- **Наблюдаемость: «сигнал жизни» (dead-man's switch).** Ловит то, чего Sentry не видит по
  определению: OOM, падение ноды, зависание — мёртвое приложение отчёт не пришлёт. Приложение
  периодически стучится на внешний бесплатный сервис; перестало — приходит тревога. **Не требует
  открывать порты наружу** — важно при нашей схеме без ingress.
- **Антибот (не гео):** `bid.cars` отдаёт `403` даже из РБ — решать при добавлении источника.
- **Бюджет прокси + backoff на бан** (из Фазы 4) — когда появится платный прокси.
- **Поднять частоту проверок** (раз в сутки → чаще) — скорость уведомления главное
  конкурентное поле. Заодно пересмотреть порог «мёртвой ссылки»: сейчас 5 неудач = 5 дней.
- **Окружения dev/prod:** сейчас **одно** (деплой с `dev`). Позже — два namespace + Kustomize
  overlays; требует отдельного бота (один токен = один поллер) и отдельной БД.
- **Секреты в git (SOPS + age)** — см. ниже.
- **Позже, для обучения — Postgres в кластере.** Прод сидит на managed (Supabase), но
  для практики поднять свой Postgres в k8s — **сначала на dev-окружении** (не рискуем
  прод-данными): Deployment + **PVC** (постоянный том) + Service + пароль в Secret; затем,
  как следующий уровень, оператор **CloudNativePG** (бэкапы в object storage, PITR;
  HA/failover — на мульти-ноде). Отличный stateful-урок k8s. Переход обратим
  (`DATABASE_URL`), код не меняется.
- **При следующем добавлении секрета — завести хранилище у владельца.** Сейчас значения живут
  только в кластере: пересборка → восстанавливать неоткуда. Момент удобный именно тогда, когда
  добавляется новый секрет (заводим хранилище и заодно переносим остальные). Выбран
  **KeePassXC**: отдельный файл базы = проектные секреты изолированы от личных паролей (не папка
  в общем хранилище, а другой файл со своим паролем), есть папки под окружения и вложения — туда
  же ляжет SSH-ключ. Бесплатен, офлайн; синхронизация — файлом в iCloud Drive.
  Подробности — [DEPLOY.md](DEPLOY.md).
- **Позже — секреты в git (SOPS + age).** Сейчас секреты живут **только в кластере** и создаются
  скриптом (`deploy/secrets.sh`): пересобрал кластер — восстанавливай вручную из менеджера паролей.
  Это неудобство, а не потеря (каждый секрет перевыпускается в своей консоли), но правильно —
  хранить их **зашифрованными в git** рядом с манифестами: восстановление = `apply` из репозитория,
  история изменений видна. Ключ расшифровки — в менеджере паролей. ⚠️ Обратная сторона: потеря
  этого ключа делает зашифрованное нечитаемым, так что «сложнее» ≠ «безопаснее» автоматически.
  Естественно совмещается с GitOps-шагом ниже.
- **Позже, для обучения — GitOps (ArgoCD / Flux).** Сейчас деплой «пушим» из CI по SSH —
  для одного приложения это оправданно. Следующий уровень: оператор **внутри** кластера сам
  тянет желаемое состояние из git. Что это даёт: входящий доступ не нужен вовсе (никаких
  CD-ключей), кластер **сам исправляет расхождения** (кто-то поменял руками — вернётся к git),
  git становится единственным источником правды с готовым аудитом и откатом. Цена: ещё один
  сервис в памяти ноды и больше движущихся частей. **Делаем ради практики** — естественный
  момент: когда появятся dev/prod окружения (Kustomize overlays) и/или второй сервис.

### Фаза 6 — Неподдерживаемая ссылка → GitHub Issue → LLM-адаптер

- Распознавать и «голые» домены без `http://`; явное сообщение о неподдерживаемом
  источнике + кнопка «Запросить источник».
- Неизвестная ссылка → спросить «что детектить» → GitHub Issue по шаблону
  (url + описание + telegram_id заявителя).
- Ревью человеком → Claude генерит адаптер по контракту + фикстурам → уведомить заявителя.

### Фаза 7 — UX-апгрейды

- Доставка всего дайджеста батчами (несколько сообщений, общий кап ~100) вместо
  «…and N more»; кнопка «Показать ещё» для «Show current».
- **Обратная связь:** команда/кнопка «Написать в поддержку» и «Предложить идею»
  (сообщение уходит владельцу / в issue-трекер).
- Уточнить копи baseline: «сейчас N (это база, не шлю); новые буду присылать».
- Выбор event-типов на подписку.
- Частота на подписку: instant (~15–30 мин) / daily digest; **тихие часы** (не слать ночью).
- Пост-фильтры на подписку: min/max цена, ключевые слова, «только с фото»;
  ценовые триггеры («цена ниже X / упала на N%»).
- «Избранное»: следить за конкретным объявлением (цена/снятие); кнопки на карточке
  уведомления — ⭐ избранное / 🙈 скрыть.
- Индикатор здоровья в `/list`: «проверено сегодня 09:00, новых: 2» у подписки.
- Приватность: `/delete_me` (удалить все свои данные) + экспорт подписок.
- Валютное предпочтение BYN/USD на юзера.
- **Deep-links** (`t.me/bot?start=…`): кнопка «Поделиться поиском» (подписка в 1 тап),
  реферальный трекинг источников юзеров; связка с постами каналов.
- **Месячное подтверждение**: одно сообщение со списком подписок, «✅ Оставить все» /
  выборочно; нет ответа N дней → автопауза + возврат одной кнопкой (экономит скрейпинг).
  Позже — спрашивать только неактивных.
- **Цель доставки на подписку** (`deliveryTarget`): личка | свой канал | группа+топик
  (бот-админ сам заводит топик на подписку). Тот же механизм — для кураторских каналов.
- **Публичные кураторские каналы** (владелец): авто-постинг подборки по фикс-ссылке
  в публичный канал (напр. «Участки Гродно») → набор подписчиков → монетизация
  рекламой (Фаза 9). Параллельно с личными подписками.
- Telegram Mini App.
- **i18n / переводы:** сейчас все сообщения бота на английском (единый исходный язык),
  аудитория пока русскоязычная, дальше — англоязычная. Позже ввести механизм переводов
  (локаль на юзера, ключи вместо строк) и русский перевод.

### Фаза 8 — AI в чате

- NL-запросы по данным пользователя (статистика и пр.) через tool-calling над БД,
  актуальные модели Claude.
- Weekly summary как retention-хук: «по твоему поиску: N новых, медиана цены X,
  −3% за месяц» (на базе price history).

### Фаза 9 — Монетизация

- Публичные каналы → **1000+ подписчиков → официальный Telegram Ads** (50% revenue
  share; у недвижимости CPM $12+) + продажа постов/партнёрки.
- Изоляция каналов владельца: лимит ~10 публичных юзернеймов на аккаунт (Premium 20) →
  отдельный «бизнес»-аккаунт как владелец, посты делает бот; папки+архив+mute.
- Реклама в уведомлениях/дайджесте, тарифы/лимиты.

### Фаза 10 — Админ-дашборд (React/Next.js)

Read-only админка для обзора всего сервиса (не для юзеров — для владельца/оператора).
Зависит только от Фазы 3 (БД) + HTTP-API — можно вынести раньше при необходимости.

- Отдельное Next.js/React-приложение (в этом репо, напр. `apps/admin`, или отдельный
  сервис) поверх read-API от бэкенда (NestJS-контроллеры, защита — токен/basic-auth,
  только чтение на старте).
- Экраны: пользователи (профиль, подписки, активность), подписки (URL, источник,
  статус baseline/«мёртвая», последний прогон), прогоны/доставки, ошибки скрейпа,
  графики продуктовых метрик (переиспользовать Prometheus/OTel из Фазы 4).
- Позже: действия (пауза/удаление подписки, ретрай прогона, бан-абьюз), просмотр
  price history/снимков, аудит.

### Фаза 11 — Описание репо под продукт (финал)

- Обновить `README.md` и `package.json` (name/description) — всё ещё описывают
  скелет; финально вычитать llm-доки.

## 4. Сквозные задачи

- Поддерживать `PRODUCT.md` и этот план в актуальном состоянии при изменениях.
- Тесты на адаптеры (фикстуры), e2e на бот-флоу.
- Секреты — через k8s Secrets / env, не в репо.
- Анти-абьюз: лимиты (подписок на юзера, без дублей), валидация URL, защита от спама.
- Держать бесплатно/дёшево: искать бесплатные/дешёвые прокси; потолок бюджета скрейпинга.
- Легальность/ToS скрейпинга для публичного сервиса — осознать риски.

### Технический бэклог (из ревью 2026-07)

Отложено осознанно — сделать при ближайшем касании соответствующего кода:

- **realt: страницы с `apolloState`.** Вживую часть поисковых URL рендерит
  `pageProps.apolloState` без `objects` — парсер видит их как пустые. Выяснить,
  какие URL так отвечают, и поддержать эту форму (или перейти на API realt).
- **`fetchHtml`: потоковый size-cap.** Chunked-ответ без `Content-Length` сейчас
  буферизуется целиком до проверки лимита; читать потоково с ранним abort.
- **Массовый auto-pause при простое источника (UX-решение).** Если источник лежит
  ≥5 прогонов, каждая его подписка авто-паузится и каждому юзеру уходит ложное
  «обнови ссылку», хотя URL живы (админ уже получил алерт о простое источника).
  Развести source-outage и dead-link: при простое всего источника не паузить его
  подписки по dead-link-счётчику.
- **Cross-user дедуп источников.** `runDaily` опрашивает URL каждой подписки
  отдельно — два юзера на один normalized URL бьют источник дважды. Целевой дизайн
  (PRODUCT.md § «Как работает внутри»): опрашивать уникальные URL раз за прогон.
- **realt: `linkPath` фолбэк на `'sale'`.** Ссылка на объект собирается из URL
  поиска; поиск без сегмента `(sale|rent)/(type)/` даёт фолбэк `sale` → возможен 404. Брать deal-type из данных объекта либо сделать нерезолвимый slug «громким».
- **Реентранси `runDaily`.** Нет overlap-guard: при `WATCH_CRON` чаще, чем длится
  прогон, возможны параллельные прогоны с гонкой на `markSeen`. Добавить флаг
  «уже идёт».
- **`markSeen`+`pruneSeen` в одной транзакции** (как `seedBaseline`) — сейчас на
  базовом менеджере; сбой между вставкой и prune оставляет таблицу непрунутой до
  следующего вызова (самолечится, но несогласованно с sibling-методом).
- **Спека на `next-data.ts`** — три null-ветки (нет open-тега / нет `</script>` /
  JSON бросил) сейчас без прямого теста.
- **Объект-параметры для swappable-пар** — `jitteredDelay(minMs, jitterMs)` и
  `WatchMetrics.setTotals(users, active)`: две смежных `number` можно перепутать
  на вызове (code-style § Function Parameters).
- **`WATCH_CRON`: настоящая cron-валидация** в env-схеме (сейчас только «5 полей»;
  мусор валит бут позже, в конструкторе CronJob).
- **Pending-подтверждения: TTL** (сейчас только cap 500, протухшие живут до
  вытеснения) — вместе с DB-слайсом.
- **Тестируемость бота:** инжектируемая фабрика `Bot`, чтобы покрыть enabled-путь
  `TelegramService` (start/notify); e2e на throttler (429).
- **CI:** порог coverage; `npm audit` без `continue-on-error`; smoke-тест
  собранного Docker-образа (`docker run` + health-check); dependabot
  docker-ecosystem (пиннинг/бампы базовых образов `node`/`postgres`).
- **OTel:** дождаться flush в shutdown-хуке (сейчас `void sdk.shutdown()`).
- **Runbook переезда на новый хост дочитать до конца.** В §«Переезд» не сказано двух вещей,
  которые появились вместе с усилением деплоя: `deploy.sh` ставится из `origin/$REPO_REF`
  (локальная правка не доедет без push) и `setup-server.sh` нельзя запускать из самого
  `$REPO_DIR` — его force-checkout перепишет файл, который читает bash. Оба факта пока живут
  только комментариями в скрипте.
- **Одноразовый переход `db-ca`.** На текущем кластере ConfigMap был создан руками; первый
  `apply -k` его перенимает и перезаписывает, а смонтированный сертификат подхватывается
  только при перезапуске пода. Содержимое совпадает байт в байт, так что делать ничего не
  нужно — но сам факт перехода нигде не описан.
- **Окружения GitHub заведены, но не используются.** `development` и `production` существуют
  без правил защиты и без привязки к веткам; в каждом лежит `SCRAPER_API_KEY`, которого нет
  нигде в коде. Решить: если ключ живой — это висящий доступ (никто не пользуется, никто не
  следит), удалить или задействовать. Заодно окружения — готовый способ закрыть отсутствие
  гейта подтверждения перед прод-деплоем: привязать джобу `deploy` к `production` с правилом
  «нужен подтверждающий». Переносить CD-секреты при этом не нужно:
  джоба с `environment:` видит и репозиторные, и окружения; окружение переопределяет только
  при совпадении имени.
- **Место `report.ts`.** Лежит в `modules/telegram/`, но импортирует `SourceUnavailableError`
  из `modules/sources/` — сквозная забота внутри одного модуля. Пока потребитель один, это
  нормально; появится второй (не-telegram) — переносить в `common/`.
- **ConfigMap через `configMapGenerator`.** Сейчас у ConfigMap фиксированное имя, поэтому
  `apply -k` правит его на месте, и автооткат деплоя (`rollout undo`) конфиг не возвращает —
  откатывается только шаблон пода. С `configMapGenerator` имя несёт хеш содержимого, версия
  конфига едет внутри шаблона пода и откатывается вместе с ним.
- **Health-путь захардкожен в двух файлах.** `/api/v1/health/{live,ready}` прописан в
  `HEALTHCHECK` образа (`Dockerfile`) и в трёх пробах `k8s/deployment.yaml`, а реально
  собирается из `API_PREFIX`/`API_VERSION` в ConfigMap. Рассинхрон → пробы стучатся в 404,
  k8s считает живой под мёртвым и перезапускает его по кругу. Сейчас держится на
  комментариях-предупреждениях. (`docker-compose.yml` путь не дублирует — наследует
  `HEALTHCHECK` образа.)
- **TypeScript 7** (dependabot PR #48 закрыт 2026-08-05). Мажор с новым компилятором;
  сейчас установка падает (`ERESOLVE`): `ts-jest@29` не принимает `typescript@7` как peer.
  Обновлять отдельным проходом, когда подтянутся `ts-jest` / `typescript-eslint` / `nest build` —
  красный PR в открытых висеть не должен, dependabot откроет новый на совместимой версии.

## 5. Из land-scraper: взять / учесть

Уже портировано: парсинг kufar/realt + нормализация цен (BYN/USD); фикстуры в `__tests__`.

Привнести позже:

- **Фото в уведомлениях** (sendPhoto / media-group) — сейчас только текст. (Фаза 7 / раньше.)
- **Детект removed / price-change** (правило «обе валюты изменились») — Фаза 2.4.
- **kufar: перейти на официальный search-API** (`api.kufar.by`, как в land-scraper)
  вместо HTML — легче и стабильнее.
- **Puppeteer/rebrowser + scrapfly** для сайтов за Cloudflare (bid-cars и т.п.): адаптер
  может внутри `fetch` использовать браузер — контракт это позволяет.
- **Больше адаптеров:** Onliner / Domovita / Hata.by (топ по трафику РБ после
  kufar/realt), av.by, bid-cars, pogorany, ghb, mosty-jobs (вакансии).
- **Алерт админу в Telegram** при поломке адаптера/скрейпа (`notifyError`) — Фаза 4.
- **Lookback-окно** как оптимизация пагинации (вместо только page-cap).
- **Кросс-сорс дедуп** (одно объявление на нескольких сайтах) — если понадобится.
